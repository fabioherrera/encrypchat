# Shared by the website image (root Dockerfile) and the relay
# (services/relay/Dockerfile, context = repo root). Do not ignore
# crates/ or services/: the relay COPY needs them. The 58 kB Dokploy
# context that then failed on those paths was this file.
.git
.tools
.cursor
**/node_modules
**/target
**/.next
**/out
**/build
apps/client
docs
*.md
!apps/web/nginx.conf
.env
.env.*
!.env.example
encrypchat logo.png
