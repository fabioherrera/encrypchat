# Place architecture folders here after `cargo ndk` / NDK build, e.g.:
#   arm64-v8a/libencrypchat_core.so
#   armeabi-v7a/libencrypchat_core.so
#   x86_64/libencrypchat_core.so
# See docs/phase-3.md
